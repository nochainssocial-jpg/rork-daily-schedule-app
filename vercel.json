{
  "buildCommand": "bun install && npx expo export -p web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "bun install",
  "functions": {
    "backend/hono.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/backend/hono.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}